# nginx-ui.conf
server {
    listen 80;
    listen [::]:80;
    root /var/www/html/;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location /api/ {
        set $upstream_backend localhost;
        proxy_pass http://$upstream_backend:8080;
    }
}
